spring:
    application:
      name: eureka-server
server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  file:
    name: ../logs/eureka-server.log

management:
  endpoints:
    web:
      exposure:
        include: "*"

